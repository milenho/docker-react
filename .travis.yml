language: generic 

sudo: required
services:
    - docker
    
before_install:
    - docker build -t milenho/docker-react -f Dockerfile.dev .

script:
    - docker run -e CI=true milenho/docker-react npm run test -- --coverage